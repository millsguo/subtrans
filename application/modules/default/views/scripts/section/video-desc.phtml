<?php
if (!empty($this->nfoData['plot'])) {
    echo '<div class="callout callout-info"><h6>剧情简介</h6><p>' . $this->nfoData['plot'] . '</p></div>';
}
if (!empty($this->nfoData['outline']) && $this->nfoData['plot'] !== $this->nfoData['outline']) {
    echo '<div class="callout callout-info"><p>' . $this->nfoData['outline'] . '</p></div>';
}